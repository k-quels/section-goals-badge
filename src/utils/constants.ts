/**
 * Maximum number of segmented bars to display for overflow progress (e.g. 5 means up to 500% overflow / 600% total).
 */
export const MAX_OVERFLOW_SEGMENTS = 5;

/**
 * Percentage range represented by each overflow segment (100% per segment).
 */
export const OVERFLOW_SEGMENT_RANGE = 100;
