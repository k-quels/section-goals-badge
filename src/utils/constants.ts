/**
 * Progress and overflow bar constants
 */
export const MAX_OVERFLOW_SEGMENTS = 5;
export const OVERFLOW_SEGMENT_RANGE = 100;

/**
 * Timer and debounce durations in milliseconds
 */
export const TYPING_DEBOUNCE_MS = 300;
export const FRONTMATTER_SAVE_DEBOUNCE_MS = 600;
export const LONG_PRESS_DURATION_MS = 600;
export const MODAL_OPEN_DELAY_MS = 60;

/**
 * UI and interaction constants
 */
export const DRAG_THRESHOLD_PX = 6;
export const MIN_BADGE_OFFSET_PX = 4;
export const DEFAULT_FONT_SIZE_PX = 12;

/**
 * Progress color threshold defaults (in percentages)
 */
export const DEFAULT_THRESHOLD_WARN = 50;
export const DEFAULT_THRESHOLD_GOOD = 80;
export const DEFAULT_THRESHOLD_DONE = 100;

/**
 * Preset color style IDs
 */
export const PRESET_STYLE_LIMIT_ID = 1;
export const PRESET_STYLE_TARGET_ID = 2;
